@model IEnumerable<SistemaClinico.Entities.Models.ReasonChangesEntity>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Order.Description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Status)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DateChanges)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Order.Description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Status)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DateChanges)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ReasonChangesID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ReasonChangesID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ReasonChangesID })
        </td>
    </tr>
}

</table>
